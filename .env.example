# Gemini Vision/추천 API 키 (선택 사항)
# 없으면 앱이 자동으로 브라우저 내 OCR(간식 인식) / 규칙 기반 추천(운동 루틴)으로 대체합니다.
# Vercel에 배포할 땐 프로젝트 Settings > Environment Variables에 동일한 이름으로 등록하세요.
# 절대 브라우저로 전달되지 않고, api/*.ts 서버리스 함수에서만 사용됩니다.
GEMINI_API_KEY=

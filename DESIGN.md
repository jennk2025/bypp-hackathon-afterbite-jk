# AFTERBITE 디자인 요약

## 한 줄 소개

먹은 간식을 사진이나 검색으로 가볍게 기록하고, 지금 상황(시간·장소·강도)에 맞는 움직임 루틴을 추천받아 타이머로 실행하는 반응형 웹앱입니다. 벌충이나 죄책감을 주는 앱이 아니라, 기록과 움직임을 가볍게 비교해서 보여주는 참고용 서비스를 지향합니다.

## 화면 구성

### 0. 공통 — 상단 헤더
- 모든 화면 최상단에 고정(fixed)되는 얇은 바 형태
- 좌측에 원형 로고 배지("A", 청록→아쿠아 그라데이션) + "AFTERBITE" 워드마크(Syne 폰트, 넓은 자간)
- 배경은 전체 페이지보다 한 톤 밝은 블루 계열로 채워, 스크롤 시 콘텐츠 위에 떠 있어도 경계가 또렷하게 구분됨
- 헤더 상단에는 은은한 블루-바이올렛 틴트가 위에서 아래로 번지는 그라데이션을 얹고, 배경 블러(backdrop blur) 처리로 아래 콘텐츠가 살짝 비쳐 떠 있는 느낌을 줌

### 1. 홈 — 오늘의 간식 트레이
- 헤더 아래 큰 타이틀 "AFTERBITE"(청록→아쿠아 그라데이션 텍스트) + 부제 문구
- 게이지 위 **캐릭터 on/off 스위치**: "남성"/"여성" 같은 글자 라벨 없이, 알약 모양 토글을 눌러 게이지 실루엣을 남성↔여성 러너 이미지로 전환
- 중앙 **달리는 사람 실루엣 에너지 게이지**: 먹은 만큼 물결이 차오르고, 완료한 움직임만큼 가라앉음
  - 남성 캐릭터는 원본 사진 비율상 작게 보이던 것을 발밑 기준으로 확대하고, 위쪽 토글 버튼과 겹치지 않도록 살짝 아래로 내려 배치
  - 실루엣 뒤에는 상태 색에 따라 번지는 후광 블롭 + 은은하게 커졌다 작아지는 이중 링이 감싸고 있어, 어두운 배경 속에서도 인물이 도드라져 보임
- 게이지 바로 아래에는 현재 에너지 상태를 나타내는 짧은 상태 문구(이모지 + 한 줄 멘트)가 있고, 그 아래 "현재 참고 에너지" kcal 숫자, "오늘 기록한 간식" / "완료한 움직임" 개수, 물결의 의미(그리고 많이 찰수록 아직 못 움직인 양이 쌓였다는 뜻)를 설명하는 안내 문구
- (진행 중인 운동이 있을 때) 이어하기 배너 — 루틴명·경과시간, "그만두기" / "이어하기" 버튼
- (완료 직후) 완료 알림 배너 — "완료 취소" / "닫기" 버튼
- "오늘의 간식 트레이" 섹션 제목 + "전체 선택" / "+ 간식 추가" 버튼
- **간식 카드 목록**: 종류별 이모지 아이콘, 제품명, 상태 배지(아직 안 바꿈 / 움직이는 중 / 움직임 완료), 섭취량·제공량, 칼로리, 수정·삭제 아이콘, 선택 체크
- 하단 고정 **선택 바**: 선택한 개수·총 칼로리 + "움직임으로 바꾸기" 버튼 (1개 이상 선택 시에만 나타남)

### 2. 간식 추가/수정 모달
- 진입 선택: "사진으로 인식하기" / "직접 입력하기" 버튼
- 사진 분석 중 화면: 업로드한 사진 미리보기 + 로딩 표시 (분석 끝나면 사진은 삭제)
- 입력 폼: 안내 문구, 제품명 입력(자동완성 제안 포함), 칼로리·1회 제공량 입력, "먹은 양" 프리셋(반/한/두 개 또는 봉지·캔 등 단위에 맞게 자동 표시) + 직접 입력, 총 칼로리 표시, 저장 버튼

### 3. 움직임 조건 선택 화면
- 선택한 간식 개수·총 칼로리 표시
- 운동 가능 시간(5/10/15/20분+ 프리셋 + 직접 입력), 장소, 강도, 소음 가능 여부, 점프 가능 여부 선택 버튼들
- "루틴 추천받기" 버튼

### 4. 루틴 추천 화면
- 예상 소모 칼로리는 범위이며 개인차가 있다는 안내 문구
- 루틴 카드 3개: 이름, 예상 소요 시간, 예상 소모 칼로리 범위, 동작 미리보기, "이 루틴 시작하기" 버튼

### 5. 운동 타이머 화면
- 루틴명, 목표 시간·예상 소모 범위
- 큰 원형 타이머(경과 시간)
- "지금 동작" 강조 표시 + "다음 동작" 목록, "다음 동작으로" 버튼
- "일시정지 / 다시 시작", "운동 완료", "취소하고 나가기" 버튼

## 색상 (다크 테마)

| 용도 | 색상 |
|---|---|
| 배경 | 딥 네이비-인디고 그라데이션 (상단 `#202f57` → 하단 `#080b17`) + 중앙 상단 블루-바이올렛 은은한 스팟 글로우 |
| 헤더 배경 | 본문보다 밝은 블루 (`#30447a`), 위쪽에 옅은 바이올렛 틴트 오버레이 |
| 카드 배경 | 짙은 슬레이트 (`#101828`) |
| 본문 텍스트 | 오프화이트 (`#eef1f7`) |
| 보조 텍스트 | 밝은 그레이블루 (`#b3bcd4`) — 이전보다 밝게 조정해 라벨·설명 문구가 배경에 묻히지 않도록 함 |
| 기본 포인트(청록 계열) | 청록 (`#2dd4bf`) → 아쿠아 (`#38bdf8`) 그라데이션 |
| 서브 포인트 | 라임 (`#a3e635`) / 짙은 라임 배경 (`#1c2b10`) |

버튼 등 강조 요소는 청록→아쿠아 그라데이션을 쓰고, 배경에는 청록·라임 톤의 은은한 블러 장식(모서리 2개 + 중앙 은은한 스팟)을 깔아 어두운 화면이 답답하지 않게 했습니다.

## 에너지 게이지의 상태별 컬러 시스템

게이지가 얼마나 찼는지(먹은 만큼 쌓이고, 움직인 만큼 가라앉는 정도)에 따라 물결·후광·링의 색과 속도가 3단계로 바뀌어, "많이 찰수록 아직 덜 움직였다"는 의미가 색으로도 직관적으로 전달되도록 했습니다.

| 단계 | 채워짐 기준 | 느낌/멘트 | 색 | 물결 속도 |
|---|---|---|---|---|
| 가벼움 | 0~34% | 🌱 가벼운 상태예요 | 청록 → 아쿠아 (차분한 기본 톤) | 느림 (7초 주기) |
| 쌓이는 중 | 35~69% | ⚡ 슬슬 쌓이고 있어요 | 앰버/주황 계열 | 중간 (5초 주기) |
| 많이 쌓임 | 70~100% | 🔥 많이 쌓였어요 | 코랄 → 레드 계열 | 빠름 (3.2초 주기) |

이 색은 물결 자체뿐 아니라 실루엣 뒤 후광 블롭, 이중 링, 박스 글로우에도 동일하게 적용되어 게이지 전체가 하나의 색 톤으로 통일감 있게 반응합니다. 단계가 올라갈수록 펄스(링이 커졌다 작아지는 속도)도 함께 빨라져 시각적 긴장감을 더합니다.

## 글꼴

- **브랜드 타이틀(헤더 로고, 홈 화면 큰 타이틀)**: Syne (굵고 존재감 있는 디스플레이 서체)
- **숫자·강조 텍스트(kcal 수치, 타이머)**: Space Grotesk (볼드 지오메트릭 스타일)
- **본문**: Pretendard (시스템에 없으면 -apple-system, Segoe UI, Roboto 등 시스템 기본 글꼴로 대체)

## 접근성/모션

- 실루엣 후광 링과 물결 애니메이션은 `prefers-reduced-motion` 설정 시 자동으로 멈추도록 처리되어 있습니다.

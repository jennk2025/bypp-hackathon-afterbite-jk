# AFTERBITE 디자인 요약

## 한 줄 소개

먹은 간식을 사진이나 검색으로 가볍게 기록하고, 지금 상황(시간·장소·강도)에 맞는 움직임 루틴을 추천받아 타이머로 실행하는 반응형 웹앱입니다. 벌충이나 죄책감을 주는 앱이 아니라, 기록과 움직임을 가볍게 비교해서 보여주는 참고용 서비스를 지향합니다.

## 화면 구성

### 1. 홈 — 오늘의 간식 트레이
- 상단 타이틀 "AFTERBITE" + 부제 문구
- 게이지 위 **캐릭터 on/off 스위치**: "남성"/"여성" 같은 글자 라벨 없이, 토글 스위치를 눌러 게이지 실루엣을 남성↔여성 러너 이미지로 전환
- 중앙 **달리는 사람 실루엣 에너지 게이지**: 먹은 만큼 물결이 차오르고, 완료한 움직임만큼 가라앉음
- 게이지 아래 "현재 참고 에너지" kcal 숫자, "오늘 기록한 간식" / "완료한 움직임" 개수, 물결의 의미를 설명하는 안내 문구
- (진행 중인 운동이 있을 때) 이어하기 배너 — 루틴명·경과시간, "그만두기" / "이어하기" 버튼
- (완료 직후) 완료 알림 배너 — "완료 취소" / "닫기" 버튼
- "오늘의 간식 트레이" 섹션 제목 + "전체 선택" / "+ 간식 추가" 버튼
- **간식 카드 목록**: 종류별 이모지 아이콘, 제품명, 상태 배지(아직 안 바꿈 / 움직이는 중 / 움직임 완료), 섭취량·제공량, 칼로리, 수정·삭제 아이콘, 선택 체크
- 하단 고정 **선택 바**: 선택한 개수·총 칼로리 + "움직임으로 바꾸기" 버튼 (1개 이상 선택 시에만 나타남)

### 2. 간식 추가/수정 모달
- 진입 선택: "사진으로 인식하기" / "직접 입력하기" 버튼
- 사진 분석 중 화면: 업로드한 사진 미리보기 + 로딩 표시 (분석 끝나면 사진은 삭제)
- 입력 폼: 안내 문구, 제품명 입력(자동완성 제안 포함), 칼로리·1회 제공량 입력, "먹은 양" 프리셋(반/한/두 개 또는 봉지·캔 등 단위에 맞게 자동 표시) + 직접 입력, 총 칼로리 표시, 저장 버튼

### 3. 움직임 조건 선택 화면
- 선택한 간식 개수·총 칼로리 표시
- 운동 가능 시간(5/10/15/20분+ 프리셋 + 직접 입력), 장소, 강도, 소음 가능 여부, 점프 가능 여부 선택 버튼들
- "루틴 추천받기" 버튼

### 4. 루틴 추천 화면
- 예상 소모 칼로리는 범위이며 개인차가 있다는 안내 문구
- 루틴 카드 3개: 이름, 예상 소요 시간, 예상 소모 칼로리 범위, 동작 미리보기, "이 루틴 시작하기" 버튼

### 5. 운동 타이머 화면
- 루틴명, 목표 시간·예상 소모 범위
- 큰 원형 타이머(경과 시간)
- "지금 동작" 강조 표시 + "다음 동작" 목록, "다음 동작으로" 버튼
- "일시정지 / 다시 시작", "운동 완료", "취소하고 나가기" 버튼

## 색상 (다크 테마 리뉴얼)

| 용도 | 색상 |
|---|---|
| 배경 | 딥 네이비블랙 (`#080c16`) |
| 카드 배경 | 짙은 슬레이트 (`#101828`) |
| 본문 텍스트 | 오프화이트 (`#eef1f7`) |
| 보조 텍스트 | 그레이블루 (`#8d96ab`) |
| 강조·물결·버튼 그라데이션 | 청록 (`#2dd4bf`) → 아쿠아 (`#38bdf8`) |
| 포인트 | 라임 (`#a3e635`) / 짙은 라임 배경 (`#1c2b10`) |

버튼 등 강조 요소는 청록→아쿠아 그라데이션을 쓰고, 배경에는 청록·라임 톤의 은은한 블러 장식(모서리 2개 + 중앙 은은한 스팟)을 깔아 어두운 화면이 답답하지 않게 했습니다.

## 글꼴

숫자·타이틀 등 강조 텍스트는 **Space Grotesk**(볼드 지오메트릭 스타일)를, 본문은 **Pretendard**(시스템에 없으면 -apple-system, Segoe UI, Roboto 등 시스템 기본 글꼴로 대체)를 사용합니다.
